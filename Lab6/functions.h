#pragma once
#include <iostream>


void PrintArray(int*, int*, std::string); // функция вывода массива на экран
bool InArray(int*, int*, int); //функция проверки вхождения элемента в массива
void ArrayInstance(int*, int, bool); // Инициализация массива
int* FirstMax(int*, int*);
